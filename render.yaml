services:
  - type: web
    name: mediguide-ai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: GROK_API_KEY
        sync: false   # paste your xai-... key in Render dashboard
